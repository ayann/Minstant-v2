<%= render "dashboard/shared/user_links" %>
<div class="row"><%= render('layouts/messages') %></div>  
<div class="row" id="show-ou">
  <div class="large-12 columns">
    <div id="titre" class="green">
      <h1>Recaputilatif du compte</h1>
    </div>
    <div class="content">
      <span><i class="fa fa-meh-o"></i> <strong>Nom et prénom :</strong><span><%= @openfire_user.name %></span></span>
      <span><i class="fa fa-user"></i> <strong>Pseudo : </strong><span><%= @openfire_user.username %></span></span>
      <span><i class="fa fa-users"></i> <strong>Groupe : </strong><span><%= @openfire_user.groups %></span></span>
      <span><i class="fa fa-envelope-o "></i> <strong>Email : </strong><span><%= @openfire_user.email %></span></span>
      <% if session[:password] %>
        <span><i class="fa fa-ticket "></i> <strong>Pass : </strong> <span><%= session[:password]%></span></span>
        <% session.delete(:password) %>
      <% end %>
    </div>
    
  </div>
</div>

