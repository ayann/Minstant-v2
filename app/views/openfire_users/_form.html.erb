<%= simple_form_for @openfire_user do |f| %>
	<%= f.error_notification %>
	<%= f.input :name, label: 'Nom et Prénom',required:true %>
	<%= f.input :username, label: 'Nom d\'utilisateur',required:true %>
	<%= f.input :email, as: :email,label: 'Email',required:true %>
    <%= f.input :groups, label: 'Groupe',hint: 'Ce champ est optionel'%>
    <%= f.button :submit, :class => 'button tiny bgreen' %>
    <%= link_to 'Annuler', openfire_users_path,:class => "button tiny bgris" %>
<% end %>